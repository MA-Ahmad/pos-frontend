type stock = {
    id: string;
    vendor: string;
    product: string;
    quantity: string;
    price: string;
  };

  type product = {
    name: string;
  };

  type vendor = {
    name: string;
  };

 type initialValues = {
    vendor: string;
    product: string;
    quantity: string;
}